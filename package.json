{
  "name": "openrouter-translator",
  "version": "1.0.0",
  "private": true,
  "description": "Open-source streaming translator with a TypeScript frontend, Python backend, Supabase-ready Postgres storage, and Vercel deployment support.",
  "scripts": {
    "frontend:dev": "npm --prefix frontend run dev",
    "frontend:build": "npm --prefix frontend run build",
    "frontend:lint": "npm --prefix frontend run lint",
    "backend:dev": "cd backend && uvicorn app.main:app --reload --port 8000",
    "backend:check": "python3 -m compileall backend/app",
    "build": "npm run frontend:build",
    "lint": "npm run frontend:lint && npm run backend:check"
  }
}
